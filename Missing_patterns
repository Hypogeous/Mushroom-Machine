# using vim for visualizing missing data
# Examine data
str(Shroom_data)
summary(Shroom_data)
library(VIM)
# Overview of missing data
a <- aggr(Shroom_data)
# Barplot of missing data in V12
barMiss(Shroom_data$V12)

# Create dataframe with complete cases

# Exclude column with the missing values

# Inpute?
